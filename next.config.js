module.exports = {
  trailingSlash: true,
  // webpack: (config, { isServer }) => {
  //   if (!isServer) {
  //     config.resolve.fallback.tls = false;
  //     config.resolve.fallback.fs = false;
  //   }

  //   return config
  // },
}
